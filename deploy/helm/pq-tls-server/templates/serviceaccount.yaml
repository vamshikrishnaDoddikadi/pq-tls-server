apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pq-tls-server.serviceAccountName" . }}
  labels:
    {{- include "pq-tls-server.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.serviceAccount.automount }}
